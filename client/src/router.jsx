import { createBrowserRouter } from "react-router-dom";

const router = createBrowserRouter([
  {
    path: "/",
    element: <h1 className="text-4xl text-gray-400 bg-red-50 ">Hello</h1>,
  },
]);

export default router;
